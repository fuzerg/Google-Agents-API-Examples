# Google Gen AI SDK (Interactions API). Must be >=2.0.0; legacy SDKs like
# google-cloud-aiplatform / google-generativeai are NOT supported for interactions.
google-genai>=2.0.0

# ADC access tokens for the Interactions API (Data Plane) and the Agents
# Control Plane REST calls.
google-auth>=2.28.0

# Raw MCP connectivity preflight + Control Plane REST.
requests>=2.31.0

# Parse agent.yaml.
pyyaml>=6.0
