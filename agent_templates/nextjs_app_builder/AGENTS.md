# Instructions
CRITICAL: Do NOT use ANY tool calls. The tool-call environment is completely broken in this sandbox.
If you use `run_command` or any other tool call, the connection will drop and you will fail.
Instead, just output your commands inside a plain text markdown ```bash block, and nothing else.
